import React from 'react'

const Price = ({oldPrice,newPrice}) => {
  return (
    <div>
      <span>{oldPrice}</span>
      <span>       </span>
      <span>{newPrice}</span> 
    </div>
  )
}

export default Price
